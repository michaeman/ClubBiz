<%= form_for(@adminevent) do |f| %>
  <% if @adminevent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adminevent.errors.count, "error") %> prohibited this adminevent from being saved:</h2>

      <ul>
      <% @adminevent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :admin_id %><br>
    <%= f.number_field :admin_id %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.number_field :event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
